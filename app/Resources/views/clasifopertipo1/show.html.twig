{% extends 'base.html.twig' %}

{% block body %}
    <h1>Clasifopertipo1</h1>

    <table>
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ clasifOperTipo1.nombre }}</td>
            </tr>
            <tr>
                <th>Idclasifopertipo1</th>
                <td>{{ clasifOperTipo1.idClasifOperTipo1 }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('clasifopertipo1_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('clasifopertipo1_edit', { 'idClasifOperTipo1': clasifOperTipo1.idClasifOperTipo1 }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
