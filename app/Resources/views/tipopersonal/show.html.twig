{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tipopersonal</h1>

    <table>
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ tipoPersonal.nombre }}</td>
            </tr>
            <tr>
                <th>Idtipopersonal</th>
                <td>{{ tipoPersonal.idTipoPersonal }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tipopersonal_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tipopersonal_edit', { 'idTipoPersonal': tipoPersonal.idTipoPersonal }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
